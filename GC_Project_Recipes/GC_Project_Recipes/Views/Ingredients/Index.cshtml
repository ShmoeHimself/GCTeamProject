@model IEnumerable<GC_Project_Recipes.Models.Ingredient>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ingred_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ingred_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Ingred_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Ingred_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Ingred_ID })
        </td>
    </tr>
}

</table>
